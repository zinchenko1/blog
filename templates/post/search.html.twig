{% extends 'layouts/base.html.twig' %}
{% block title %}
    Posts Search
{% endblock %}
{% block body %}
    <div class="section">
        <div class="container">
            <h1 class="text-center pt-4">Search results for request - "{{ query }}" - {{ posts|length }} <span class="post-count"></span></h1>
            {% if posts|length > 0 %}
            <div class="posts">
                <div class="row">
                    <ul>
                        {% for post in posts %}
                            <li>
                                {#            <a href="{{ path('post_show', {slug: post.slug}) }}">{{ post.title }}</a>#}
                                <a href="#">{{ post.title }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    </div>
    {% else %}
        <p class="text-center">Posts not found.</p>
    {% endif %}
{% endblock %}
